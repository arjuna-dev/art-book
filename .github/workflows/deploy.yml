name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: art-book-vue/package-lock.json

      - name: Install dependencies
        working-directory: art-book-vue
        run: npm ci

      - name: Build
        working-directory: art-book-vue
        run: npm run build
        env:
          BASE_URL: /art-book/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: art-book-vue/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
